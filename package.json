{
  "name": "sai",
  "version": "1.0.0",
  "description": "SAI - Production Landing Page",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.11.0"
  }
}
